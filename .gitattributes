# Normalize line endings. Shell launchers MUST be LF or they won't run on
# macOS/Linux; Windows batch files must be CRLF.
* text=auto

*.sh        text eol=lf
*.command   text eol=lf
start.py    text eol=lf

*.bat       text eol=crlf
*.cmd       text eol=crlf
